services:
  bot:
    build: .
    image: dota2-checker:local
    container_name: dota2-checker
    restart: unless-stopped
    env_file:
      - .env
    environment:
      HEALTH_PORT: "8080"
      LOG_LEVEL: info
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
