# Example only — do NOT commit real tokens.
# Apply with:
#   kubectl create namespace dota2-checker
#   kubectl create secret generic dota2-checker-secrets \
#     --namespace dota2-checker \
#     --from-literal=BOT_TOKEN='your-telegram-bot-token'
#
# Or copy this file, fill values (base64), and apply — keep it out of git.
apiVersion: v1
kind: Secret
metadata:
  name: dota2-checker-secrets
  namespace: dota2-checker
type: Opaque
stringData:
  BOT_TOKEN: "replace-me"
