# Telegram BotFather token (required)
BOT_TOKEN=replace-me

# HTTP port for /healthz /readyz probes (Docker / Kubernetes)
HEALTH_PORT=8080

# Structured log level: debug | info | warn | error
LOG_LEVEL=info
